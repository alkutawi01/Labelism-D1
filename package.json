{
  "name": "labelism-d1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute labelism --remote --file=schema.sql",
    "test": "node tests/acceptance.test.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
